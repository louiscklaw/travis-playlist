language: generic

import:
  - credentials.yml

script:
  - hostname
  - mkdir /tmp/gh-pages-out

deploy:
  - provider: pages
    skip_cleanup: true
    local_dir: out
    github_token: $GITHUB_TOKEN
    local_dir: /tmp/gh-pages-out
    on:
      branch: test/update-install-hugo