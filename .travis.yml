# https://docs.travis-ci.com/user/docker/
# https://docs.travis-ci.com/user/environment-variables/
# https://docs.travis-ci.com/user/job-lifecycle/
# https://docs.travis-ci.com/user/conditional-builds-stages-jobs/
# https://docs.travis-ci.com/user/conditions-v1

# You can import up to 25 other configs in total.
os: linux

language: node_js

node_js:
  - 12

cache:
  - npm
  - pip

import:
  - credentials.yml
  - .travis-stages.yml
  # - .travis-merger.yml

  - .travis/.build.yml
  - .travis/.build_docker.yml
  - .travis/.cron.yml
  - .travis/.deploy.yml
  - .travis/.integration_to_other_repo.yml
  - .travis/.merge.yml
  - .travis/.publish_dashboard.yml
  - .travis/.test.yml
  - .travis/.test_cron.yml
  - .travis/.test_publish_dashboard.yml
  - .travis/.unit_test.yml


# after_success:
#   travis-test-after-success/fail_script.sh
