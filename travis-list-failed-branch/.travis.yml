jobs:
  include:
    # TODO: move to Cron stage
    - stage: Test_cron
      name: travis-list-failed-branch (travis-list-failed-branch/.travis.yml)
      script:
        - cd travis-list-failed-branch
        -   yarn
        -   node ./test_updateBuildFaliledList.js
        -   node ./test_update_db.js

        -   node ./index.js

        - cd ..
