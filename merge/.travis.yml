
jobs:
  include:
    - stage: Merge
      script:
        - echo "dummy job for merging"

after_success:
  - echo $TRAVIS_BUILD_STAGE_NAME
  - if [ "$TRAVIS_BUILD_STAGE_NAME" = "Merge" ]; then ./_travis/merge_if_success.sh; fi
